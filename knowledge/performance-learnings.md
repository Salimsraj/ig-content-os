# Performance learnings

Seeded empty. This is the feedback-loop file: the `performance-analyst` agent (Phase 7) is the intended
writer, appending confirmed, durable learnings here after analyzing real performance data — this is what
closes the loop from "Performance Analysis" back into future "Ideas"/"Hooks"/"Scripts" work, which the
Phase 0 audit found was entirely missing (diagnosis was shown once on the dashboard and never persisted
anywhere).

Until Phase 7 exists, this file should stay empty rather than pre-filled with guesses — a premature
"learning" here would get treated as confirmed brand knowledge by every other skill/agent that reads it.

## Format (for Phase 7 to follow)

Each entry should be a dated, falsifiable claim tied to real data, not a vague impression — e.g. "2026-XX-XX:
Reels opening with a number in the first 3 words outperformed the account median by 2.1x across N=8 posts
(see `data/viral-hook-analysis.json` / live IG insights)." — not "hooks with numbers seem to do well."
