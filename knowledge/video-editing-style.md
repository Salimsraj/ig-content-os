# Video editing style

*Empty scaffold.* No video editing capability exists in the repo yet — this is Phase 4.

To be populated once `video-editor` starts running in the hybrid edit-plan-for-approval mode (D.4): cut
pacing, silence/dead-air tolerance, punch-in/zoom conventions, B-roll usage patterns, music/audio
treatment. Preferences confirmed repeatedly during the approval loop should get written back here so the
system's default edit plans converge on your actual style over time — that convergence is what
eventually justifies switching a given edit type to full-auto mode.
